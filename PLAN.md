# 植物单细胞生信分析专家（scRNA Pipeline Expert）— 子项目执行方案 v0.4

> 创建：2026-08-19（v0.1 初稿 → 三模型审核 → v0.2 整合修正）→ v0.3：2026-08-20 融入 Biomni 思想（Science 2026, Stanford SNAP）完善
> 位置：/workspace/plant-expert-agent/scRNA-pipeline-expert/
> 上级目标：植物领域专家 Agent（长期目标）——本子项目为其"确定性执行层"

---

## 〇、项目定位（两个目标的关系）

```
植物领域专家（长期目标，已推进至骨架文献底座）
 ├── 知识层：52 篇骨架文献蒸馏 + botanical 知识库（进行中，步骤 3b 未做）
 ├── 编排层：plant-scrna-spatial-expert skill（已建成）
 └── ★ 执行层：本子项目——植物单细胞生信分析专家（确定性脚本+skill 体系）
             完成后回填编排层能力路由表，成为领域专家的"手"
```

**本子项目核心命题**：把植物单细胞转录组分析从"LLM 每次现编"升级为"固定脚本确定性执行 + LLM 只做任务拆分与流程编排"。同一种数据，任何人、任何模型跑同一任务，得到同一结果。

**系统定位（审核修正）**：可复现、可审计、可解释的**科研辅助专家**，不是黑箱自动化专家——最终报告须含 provenance、置信度、不确定性声明，不替代专家判断。

---

## 〇.5 Biomni 思想融合总览（v0.3 新增）

> 参考：`/workspace/domain-knowledge/biomni-agent-architecture.md`（Biomni: A General-Purpose Biomedical AI Agent, Science 2026, Stanford SNAP）。本方案 v0.2 的"确定性脚本 + skill 路由 + 降级链 + 评估"与 Biomni 的"E1 环境 + A1 智能体"架构同构；v0.3 补齐 Biomni 五个可迁移机制：

| # | Biomni 机制 | 思想（白话） | 本方案落地（v0.3 变更点） |
|---|---|---|---|
| B1 | E1 行动空间构建：2500 篇文献提取"任务→工具→数据库"→ 人类专家验证 | 行动空间不拍脑袋，文献驱动 + 验证 | 阶段 0 新增**行动空间文献提取**：从 52 篇骨架文献 + 方法学综述 + 教程代码库系统提取任务/工具/数据库/验证标准，与 T0-T17 交叉校验补漏 |
| B2 | A1 ① 动态工具选择：独立检索 LLM 按查询注入工具子集 | 不把全量工具塞进上下文 | 每个原子 skill 增 **tools-manifest 条目**（任务→工具链→数据库→输入输出→调用命令→验证方式），汇总为注册表（架构 L1.5）；编排层按任务检索注入，替代"凭记忆选工具"与手写候选集 |
| B3 | A1 ② 代码作为统一行动接口（CodeAct） | 每步用可执行代码表达，可组合循环/并行/条件 | 已同向（确定性脚本+流程 JSON）；v0.3 强化：流程 JSON 支持**并行节点**（多组 marker 同时鉴定）与**条件分支**（检测到批次效应才触发整合），decision 字段记录分支理由 |
| B4 | A1 ③ 自适应规划：初始计划 + 执行观测迭代修订 | 计划边做边改 | 显式化**"观测→修订"闭环**：数据依赖步骤（T3 QC 阈值/T8 整合方法/T9 分辨率）输出诊断指标 → 规则引擎判定 → 有限轮次内自动修订参数重跑 → 超限熔断转人工；每次修订写入 provenance |
| B5 | 评估：8 个真实任务 + 消融实验 | 用数据证明组件价值，而非自评 | ① 真实任务基准集：带金标准注释的公开数据集做端到端任务 ② **消融评估**（阶段 5b）：分别关闭 tools-manifest 检索/规则引擎/降级链，对比任务成功率退化 |

**差异化声明（不照搬 Biomni）**：Biomni 追求 25 个生物医学子领域的通用广度（150 工具/105 软件/59 数据库）；本专家是**植物单细胞领域纵深**——tools-manifest 规模小（约 18 任务 × 1-3 工具），但每个工具调优到植物专家级（植物特异 QC、C4 标记规则、跨物种 ortholog 桥），并内嵌领域红线（C4/C3 标记不可混用等）。通用性靠"编排层按任务检索 + 降级链"获得，不靠工具数量堆砌。

**资源策略声明（Biomni 行为规律）**：生信分析型任务 = 代码执行为主、数据库按需（标准分析走本地确定性管道）；信息综合型任务（基因注释/通路/物种基准查询）= 走知识库/数据库。编排层两条路径显式分流，不混用。

---

## 〇.6 验证优先思维融合总览（v0.4 新增，LLM-as-a-Verifier）

> 参考：`/workspace/domain-knowledge/llm-verifier-thinking.md`（LLM-as-a-Verifier: A General-Purpose Verification Framework，Stanford Scaling Intelligence Lab + Berkeley + NVIDIA, arXiv:2607.05391, MIT）。核心思想："生成"与"选择"是两件事——模型常能生成正确答案但选不出对的；把验证做成独立组件，用细粒度信号 + 标准分解去选，性能即可提升。v0.4 融入本方案的最关键落点是 **T11 注释结果可信度设计**（多候选注释 + 标准分解验证）：

| # | 验证器思维 | 思想（白话） | 本方案落地（v0.4 变更点） |
|---|---|---|---|
| V1 | 生成-选择分离 | 不追求一次生成完美，追求"候选多样 + 选择准确" | T11 注释任务产出 **2-3 个候选注释方案**（marker 双视图法 / 自动注释工具初稿 / 跨物种 ortholog 迁移），独立验证器按标准分解评分选优，不再一次成型 |
| V2 | 标准分解 + 细粒度评分 | 不笼统问"注释对不对"，拆成可独立核对的子标准逐条打分 | 注释可信度五标准：marker 证据强度 / 聚类一致性 / 文献知识库一致性 / 跨样本跨物种保守性 / 生物学合理性——每条 1-5 分 + 证据（详见"三、确定性保障机制"注释可信度行） |
| V3 | 验证缩放 + 成本工程 | 验证要便宜到可重复执行，否则只是仪式 | 评分复用 T10 marker 统计结果与 T12 映射成功率等**既有中间产物**，不重复计算；候选注释并行生成；评分表进 provenance |

**两条边界声明（不照搬验证器）**：
- 验证器提升"选择"不提升"生成"——候选生成端（T10 marker 质量/T12 映射质量）仍是基础，验证不替代 marker 鉴定质量建设
- 同模型自验证存在系统性偏差（论文 Oracle 上界 92-96% 与实际有差距）——**注释终审仍是人工/大模型审查节点**（T11 决策类定位不变），标准分解评分表只做"机械化预筛 + 争议定位"，不替代领域专家终审

---

## 一、前提想法汇总与合理性评估（逐条回应，含三模型审核结论）

### 想法 1：用真实项目逐步分析，形成固定脚本 skill（确定性结果）
**合理。但"固定脚本"≠"正确"，必须分层。** 固定脚本消除 LLM 随机性，但脚本本身可能写错、参数可能不适配新数据——会系统性复现错误，比随机幻觉更隐蔽。修正：
- 所有分析步骤分三类（方案核心分层）：
  - **确定性步骤**（固定脚本）：格式转换、标准化、HVG、PCA、UMAP、GO 富集等——算法与数据无关
  - **数据依赖步骤**（规则+统计推断，脚本内置规则引擎）：QC 阈值（按分布/MAD 推荐）、HVG 数量、批次整合方法选择（先检测批次效应再决定）、分辨率选择（稳定性评估）——**参数不硬编码，由脚本基于当前数据计算推荐值**
  - **科学判断步骤**（人工/大模型审查）：注释合理性、marker 生物学解读、报告结论——路由到审查节点
- 金标准注释仅作重要参考（论文本身可能有争议/依赖数据库版本），正确性证据链=marker 证据+专家复核，不止"与论文一致"

### 想法 2：细粒度拆分 → 小 skill → 中性任务 skill → 终极专家，LLM 做拆分与调用
**合理且可实现。** 补充两点：
- 单细胞分析是**迭代**而非线性流水线：聚类后发现 marker 不特异需回头调整合参数。流程 JSON 须含 `decision` 字段记录每个分支的理由（如 `{"batch_integration": {"decision":"harmony","reason":"batch_effect_detected","metric":"kBET=0.42"}}`），L2 支持**回退重跑**（下游异常→回退上游节点调参重跑），不强制一次走完
- 执行类 skill 固定（格式转换/标准化/降维/GO）；**决策类 skill 必须有规则/统计检验/大模型介入**（整合方法选择、注释审查、分辨率选择）

### 想法 3：三步构建（测试数据 → 脚本拆分+学生验证 → 汇总融合测试）
**框架可行，但原验收体系不足，需补三样**：
1. **数据泛化**：单一玉米苞叶会过拟合（参数围绕它调）。构建期至少：主数据集（husk 8,980 全量+2,000 子集）+ 1-2 个独立数据集（C4_kranz 水稻/玉米已有 h5ad）+ 人工构造异常数据（缺基因/空矩阵/高叶绿体线粒体/极低细胞数/非植物基因 ID）
2. **量化验证指标**（学生验证不许"看图差不多"）：聚类 ARI/NMI、注释 top-1 准确率/F1、marker 特异性、批次整合 kBET/iLISI
3. **回归机制**：任何 skill 修改后重跑全流程 benchmark（golden test 集），防止改动一处破坏他处

### 想法 4：构建期用聪明大模型，执行期用足够小的模型串联
**部分可行，边界必须收敛**：
- 小模型的正确角色 = **受限语言前端**（意图识别→路由→参数抽取→输出 JSON），不做策略决策。可行条件：任务映射到预定义流程模板、参数候选集封闭（物种/组织/分组/阈值格点）、每步有确定性 schema
- **科学判断必须外置**（注释审查/整合方法选择/轨迹必要性/分辨率/显著性判断）→ 路由大模型或人工
- **降级链**（小模型失败不静默）：小模型解析失败 → 规则引擎 → 大模型 → 反问用户 → 人工。同一节点报错 ≥2 次即熔断转人工
- **上线量化门槛**：小模型路由准确率/参数抽取准确率/流程无干预完成率达标（建议 ≥80%，用 50-100 条植物 scRNA 指令 benchmark）才可替换大模型；未达标则维持"中模型为主+小模型意图识别"混合
- 最终形态不是"小模型串联各 skill"，而是**确定性流程引擎 + 语言前端**（DAG 主流程硬编码，LLM 只在特定节点做参数决策/结果判断）

---

## 二、总体架构（三层 + 横切注册表；v0.3 融入 Biomni 动态工具检索与观测→修订闭环）

```
L2 编排层：plant-scrna-bioinformatics-expert（专家 skill）
   任务解析（小模型语言前端）→ 动态工具检索（tools-manifest 注册表，
   按任务注入工具子集，B2）→ 流程组装（流程 JSON，含 decision 记录+回退能力）
   → 执行 → 验证 → 观测→修订闭环（B4：诊断指标→规则引擎→自动重跑→熔断）
   → 降级链（规则引擎/大模型/人工）
   ★ 判断类任务路由大模型；异常熔断转人工；全程 provenance 记录

L1 技能层：
   原子 skill ×16-18（每个对应 1-N 个确定性脚本；执行类 vs 数据依赖 vs 决策类明确标注）
   中性流程 skill ×1（标准端到端流程，固定顺序+决策节点）
   横切能力：跨物种 ortholog 映射（嵌入 ID 统一/注释/GO，非孤立 skill）
   每个 skill = SKILL.md（触发条件/参数/输出/验证命令/已知坑）+ scripts/ + tests/
   每个 skill 附 tools-manifest 条目（B2）

L1.5 横切：tools-manifest 注册表（v0.3 新增，Biomni E1/A1 ① 落地）
   结构化元数据：任务 → 工具链 → 数据库 → 输入输出 → 调用命令 → 验证方式
   编排层检索入口（替代凭记忆选工具）；数据库分两类接入：
   web-API 类（注释查询）统一查询函数 + 本地数据湖类（marker 库/ortholog 表/GO 注释）落库

L0 脚本层：确定性可执行脚本（Python/scanpy 为主，25-35 个）
   统一规范：参数 schema（argparse+校验，拒绝非法输入）、输入数据审计、
   输出 QC 报告、生物学合理范围断言、provenance 日志、固定依赖版本、种子固定
```

---

## 三、确定性保障机制（防幻觉核心设计，审核扩充版）

| 机制 | 内容 | 防什么 |
|---|---|---|
| 参数 schema 校验 | 参数类型/范围/必填/候选集封闭；非法输入拒绝执行 | 模型传错参数产生伪结果 |
| 输入数据审计 | 入口先验：raw.X 整数%、基因 ID 格式、维度、平台（barcode 格式） | 把 log 数据当 raw 重复 normalize 等历史事故 |
| 生物学合理范围断言 | 断言含生物范围（过滤后细胞数/基因数/线粒体叶绿体%/HVG 数/聚类数），非仅格式检查；失败=明确错误类型，非 Python traceback | 静默产出生物学荒谬结果 |
| golden test + 数值容差 | 测试子集+期望输出；**容差比对**（聚类 ARI≥0.95、top marker 重叠≥95%、UMAP 结构一致）而非 bit 级一致 | 脚本演化行为漂移；伪"确定性"苛求 |
| 版本与输入锁定 | 输入 h5ad 哈希、参考基因组/转录本版本、marker/GO 库版本、conda env yaml+pip freeze（进阶：容器镜像） | 依赖/参考升级改变结果 |
| 种子与线程 | 随机过程固定 seed；关键步骤 n_jobs=1 或确定性算法变体 | 同输入不同输出 |
| provenance 审计日志 | 每步记录：输入 hash/脚本版本/skill 版本/参数/环境 hash/输出 hash/LLM 模型与 prompt 版本/temperature/决策点理由，写入流程 JSON 与最终报告 | 结果不可追溯 |
| LLM 调用确定性 | temperature=0、输出 JSON schema 校验、失败重试→规则→人工 | LLM 输出不稳定 |
| 同输入重跑一致性 | `--check`：两次运行输出 hash 比对 | 非确定性操作混入 |
| 观测→修订闭环（B4，v0.3） | 数据依赖步骤输出诊断指标（QC 分布/kBET/分辨率稳定性）→ 规则引擎判定 → 有限轮次内自动修订参数重跑 → 超限熔断转人工；每次修订写 provenance | 参数一次定死/硬编码；静默产出次优结果 |
| **注释结果可信度（V1+V2，v0.4）** | T11 产出 **2-3 个候选注释方案**（marker 双视图法 / 自动注释工具初稿 / 跨物种 ortholog 迁移），验证器按**五标准分解**逐条评分（1-5 分 + 证据）：① marker 证据强度（rank_genes 显著性、特异性、方向性）② 聚类一致性（注释边界 vs 聚类结构 ARI/同质性）③ 文献/知识库一致性（与金标准、marker 库比对）④ 跨样本/跨物种保守性（类型在不同样本/物种可重复；如 BSC 类仅 second_annotation 有、first 的 MC&BSC 群名不副实）⑤ 生物学合理性（类型组成比例、已知生物学约束）——裁决：任一 ≤2 必改，均分 <3.5 触发重新注释或转人工；评分表进 provenance | 注释一次成型无质量仲裁；\"与论文一致\"的循环论证式验证（详见 01/02 知识库：自审 marker 表验证自做注释不构成准确性证据） |

---

## 四、任务拆分设计（v0.2 修订：L0 脚本 25-35 个 / L1 原子 skill 16-18 个）

技术栈：Python/scanpy 为主（现有实战脚本主流），参考 scrnaseq-reference-codebase 已验证模式；R/Seurat 脚本仅作模式参考。

| # | L1 原子 skill | 类型 | 核心内容（含 L0 脚本） | 本地已有资产 |
|---|---|---|---|---|
| T0 | 数据导入与格式归一化 | 执行 | h5ad/10x 三文件/CSV/RDS 统一转 h5ad；平台识别（barcode 格式）；FASTQ/BAM 拒绝并提示先比对 | step0_audit、rds-python-bridge 模式 |
| T1 | 输入数据审计（8 检查） | 执行 | raw.X 整数%、基因 ID、维度、标签列、PCA 批次诊断 | pre-integration-data-audit.md |
| T2 | 基因 ID 统一 | 执行+横切 | 物种基因 ID 归一、版本记录；跨物种 ortholog 映射表+成功率报告 | match_orthologs_photosynthesis.py |
| T3 | QC 过滤（植物特异） | 数据依赖 | 线粒体/叶绿体%（植物阈值≠动物 20%）、双细胞（Scrublet/DoubletFinder）、空滴 | phase2 系列、教程 QC 模式 |
| T4 | 原生质体应激评估 | 数据依赖 | HSP/伤口响应基因检测、标记或回归选项 | 经验库 scRNA 板块 |
| T5 | 细胞周期评分 | 执行 | 植物细胞周期基因（同源映射，禁直接用人类列表） | 待建（需植物同源列表） |
| T6 | 标准化 + HVG | 执行 | normalize_total+log1p；HVG 数量按数据推荐 | 教程 2-1~2-3 |
| T7 | PCA 降维 | 执行 | HVG 子集 scale 防 OOM、PCA、PC 诊断 | 大矩阵内存规避模式 |
| T8 | 批次效应评估与整合 | 数据依赖+决策 | 先检测（kBET/iLISI）再选 Harmony（≥3 数据集）/scVI（2 数据集）；过度校正检查 | harmonypy 2.0 模式、8 检查 |
| T9 | 聚类与分辨率稳定性 | 数据依赖 | Leiden 多分辨率扫描+稳定性评估，输出推荐分辨率 | 教程 3-1 |
| T10 | marker 鉴定 | 执行 | rank_genes、特异性统计、方向性筛选 | 教程 3-2/3-3 |
| T11 | 细胞类型注释 + 审查 | 决策 | marker 双视图、文献交叉、**多候选注释（2-3 方案，V1）+ 标准分解评分验证（V2）** → 评分表 → **人工/大模型审查节点**（终审） | annotation-workflow skill |
| T12 | 跨物种注释（辅助，非孤立） | 决策 | ortholog 迁移打分→marker 锚定→物种内重筛；映射成功率<阈则强制标注低置信度 | step14_cross_species_mc.py |
| T13 | DEG（pseudobulk） | 执行 | pseudobulk+样本级检验（DESeq2/PyDESeq2/OLS+BH）；群体定义审计 | step13b_pseudobulk_ols.py |
| T14 | 差异丰度分析 | 执行 | 细胞类型比例差异（非标准 DEG 的补充） | 待建（muscat/edgeR 模式） |
| T15 | GO 富集 | 执行 | Fisher+BH，植物四物种注释 | plant-go-enrichment-tool |
| T16 | 轨迹分析（可选模块） | 执行 | 发育分组内拟时序（禁跨阶段） | developmental-trajectory-analysis.md |
| T17 | 报告与可视化 | 执行 | marker 表+dotplot+核心结果表+provenance+决策日志（EN 列纯英文） | 各项目交付规范 |

跨物种 ortholog 为**横切能力**（T2/T12），不作为孤立 skill。预留空间转录组/多组学接口（Cell2location/Tangram 类，v2 扩展）。

每个原子 skill 交付：`skills/atom-T0x/SKILL.md`（标注 执行/数据依赖/决策 类型）+ `scripts/` + `tests/golden/`。

**tools-manifest 条目（v0.3，B2）**：每个原子 skill 增补结构化元数据，汇总入 `tools-manifest.yaml` 注册表（架构 L1.5）：

```yaml
T08_batch_integration:
  task: 批次效应评估与整合
  type: 数据依赖+决策
  tools: [harmonypy, scvi-tools, scanpy]
  databases: [无（本地计算）]
  inputs: [h5ad 整合前, 批次列名]
  outputs: [h5ad 整合后, 过度校正检查报告]
  validation: [kBET>=0.5, iLISI>=0.5, PC1 批次相关 |r|<0.1]
  call: python3 scripts/step08_batch_integration.py --input ... --method {harmony,scvi}
  known_pitfalls: [2 数据集禁 Harmony；过度校正检查必跑]
```

编排层按任务描述检索该表、注入工具子集，替代把 18 个 skill 全量塞进上下文；数据库按条目声明接入路径（web-API 统一查询函数 或 本地数据湖），与 Biomni E1 的数据库两类接入同构。

---

## 五、分阶段执行计划（含验收标准，审核扩充版）

### 阶段 0：资产收集与盘点 + 行动空间文献提取（v0.3 并入 B1）
- 输入：用户收集的完整脚本流程 + 本地已有 70+ 脚本 + 3 套教程代码库 + 52 篇骨架文献 + 方法学综述
- 动作 A（原）：脚本清单化（功能/技术栈/输入输出/依赖），映射到 T0-T17 任务矩阵
- 动作 B（v0.3 新增，Biomni E1 构建法）：**行动空间文献提取**——从骨架文献/方法学综述/教程代码库提取"任务→工具→数据库→验证标准"映射，与 T0-T17 交叉校验：补漏（文献有而矩阵无的任务/数据库）、去重、验证标准补全
- 交付：`script-inventory.md`（脚本→任务映射表，标明缺口）+ `action-space.md`（文献驱动的行动空间清单，与 T0-T17 差异表）
- 验收：每类任务至少 1 个可参考脚本；行动空间与任务矩阵差异全部消解或显式记录

### 阶段 1：测试数据准备
- 主数据集：maize_leaf husk h5ad（8,980 细胞）——本项目数据、有论文金标准注释可验证 T11
  - 快速测试集：随机 2,000 细胞子集（seed 固定）；完整测试集：全量
- 独立泛化集：C4_kranz 水稻/玉米已有 h5ad（跨物种、跨平台验证，先提案获批后使用）
- 异常数据构造：缺基因/空矩阵/高叶绿体线粒体%/极低细胞数/非植物基因 ID 各 1 份
- 交付：`data/` + `data/README.md`（来源/金标准/切割/seed/异常构造方式）
- 验收：主集可无错读取；金标准列可追溯；异常集全部构造完成

### 阶段 2：脚本研读与拆分
- 逐脚本研读（方法依据/参数依据/已知坑），确定原子任务边界（第四节定稿）、统一脚本模板
- 交付：`scripts/TEMPLATE.py` + 拆分定稿文档（每任务：输入/输出/参数/决策点/验证指标）
- 验收：模板在快速测试集跑通；每任务有输入输出定义与量化验证指标

### 阶段 3：原子脚本开发 + 学生逐步骤验证矫正（核心阶段）
- 每任务 TDD 式流程：
  1. Hermes/Claude Code 按模板开发（参考既有实战脚本，最小 delta）
  2. 快速测试集跑出**基准输出**（golden，hash+容差固化）
  3. **学生验证**：代码研读 → 参数依据检查 → 独立复跑 → **量化指标比对**（ARI/F1/kBET 等，见 CHECKLIST.md）→ 差异矫正并记录
  4. **独立复核**：异常数据上验证失败路径；每 4-5 个任务一轮交叉复核（非本任务开发者）
  5. 通过后注册 skill
- 交付：scripts/（25-35 个）+ skills/atom-*/ + tests/ + student-validation/records/ + 独立复核记录
- 验收：每脚本快速测试集两次运行一致；量化指标达标；异常数据失败路径明确（拒绝或报错而非静默）；验证记录 100% 覆盖

### 阶段 4：中性流程 skill 组装 + 端到端测试
- 组装 T0→T17 为 `pipeline-scrna-standard`；完整测试集端到端；**泛化集端到端**（验证不贴主数据集调参）；测试**并行节点**（多组 marker 同时鉴定）与**条件分支**（批次效应检测才触发整合）执行正确性（B3）
- 交付：中性流程 skill + 端到端报告（含 provenance 示例、decision 日志、观测→修订记录、耗时）
- 验收：无人工干预完成率 ≥95%；泛化集跑通；报告含 provenance/决策日志/修订记录；并行与条件分支结果与串行基准一致

### 阶段 5：专家汇总 + 小模型编排验证
- 建 `plant-scrna-bioinformatics-expert`（任务解析→路由→流程定制→降级链）
- **小模型 benchmark**：50-100 条植物 scRNA 指令（覆盖标准流程/参数定制/模糊表述/异常请求），测路由准确率/参数抽取准确率/完成率/错误被 schema 拦截率；与大模型对比
- 交付：专家 skill + 小模型测试报告（含失败案例分析与降级链生效验证）
- 验收：完成率 ≥80% 或明确标注"未达标维持混合模式"；失败案例 100% 被校验/降级拦截而非产出错误结果

### 阶段 5b：消融评估（v0.3 新增，Biomni 评估方法论）
- 在阶段 5 基准集上做消融：关闭 tools-manifest 检索（退化为全量/凭记忆选工具）、关闭规则引擎（退化为 LLM 直接决策）、关闭降级链（失败即报错）→ 对比任务成功率/完成率/错误拦截率
- 交付：`ablation-report.md`（各组件对任务成功率的量化贡献）
- 验收：每个保留组件的删除导致成功率显著下降（或显式记录"未达显著、组件可裁剪"）——组件价值用数据证明，不自评

### 阶段 6：与植物领域专家融合（长期目标接线）
- plant-scrna-spatial-expert 能力层路由指向本专家脚本；52 篇知识蒸馏（原步骤 3b）继续
- 交付：融合链路（知识→编排→执行）；验收：同一任务融合前后结果一致且带知识溯源

### 渐进交付路线（审核建议，避免"一次性打包"）
v1 可复现流程管道（阶段 1-4）→ v2 交互式分析助手（HITL 节点：聚类后暂停出图请确认、注释初稿请专家打分）→ v3 受监督自动化专家（阶段 5 小模型+降级链）→ v4 有限自动专家（打包，远期）

---

## 六、小模型编排设计（v0.2 修订）

**编排器接口**：
```
输入：自然语言任务描述
输出：流程 JSON（[ {task_id, script, params, input, output, decision?} ]，含决策点理由）
执行：确定性流程引擎按序调用，每步检查退出码+断言+provenance
验证：--check / golden 容差比对
观测→修订：数据依赖步骤诊断指标 → 规则引擎 → 自动修订重跑（有限轮次）→ 熔断转人工
降级链：小模型解析失败 → 规则引擎 → 大模型 → 反问用户 → 人工；节点报错≥2 次熔断
```
- 小模型只做：意图识别→**动态工具检索（tools-manifest 注册表，B2）**→参数抽取（预定义格点）→JSON 输出。不碰数据、不做判断。路由候选集 = tools-manifest 索引（自动生成），非手写列表。
- 判断类任务（T8 整合方法/T11 注释审查/T13 显著性解读）默认路由大模型；人工终审。
- LLM 调用：temperature=0 + JSON schema 校验 + 模型/prompt 版本记录。
- 打包形态：`expert-pack/` = scripts/ + skills/ + 编排器 + env.yaml + benchmark 报告 + 文档。

---

## 七、风险与对策（v0.2 扩充）

| 风险 | 对策 |
|---|---|
| 脚本教条化（固定脚本复现错误） | 三分层（确定/数据依赖/判断）；参数规则化非硬编码；金标准仅参考+证据链 |
| 粒度失控 | 原子任务=任务级定义（第四节），拆分方案经用户确认后冻结 |
| 依赖升级破坏确定性 | 环境冻结+输入哈希+容差比对（非 bit 级）+CI 回归 |
| 学生验证主观化 | CHECKLIST 量化指标（ARI/F1/kBET）+独立复核+抽查 |
| 小模型路由错误 | schema 校验兜底+降级链+上线 benchmark 门槛 |
| 单数据集过拟合 | 主集+泛化集+异常数据三套验证 |
| 跨物种注释过度承诺 | 降级为辅助 skill：只做 ortholog 映射+成功率报告，低置信度强制标注 |
| 范围蔓延（空间/多组学） | v1 仅 scRNA；空间/多组学 v2 扩展另行审批 |
| 自动化责任边界 | 系统声明"科研辅助工具"；报告含置信度/不确定性；人工终审 |
| 行动空间遗漏（v0.3） | 文献驱动提取（阶段 0 动作 B）与 T0-T17 交叉校验；tools-manifest 注册表定期对照新文献更新 |
| 观测→修订闭环失控（v0.3） | 修订限轮次（默认 2 轮）+ 每轮记录 + 超限熔断转人工；修订规则进 golden test |

---

## 八、待用户决策/提供事项

1. **脚本集**：您收集的完整脚本流程（阶段 0 输入，最优先）
2. **测试数据确认**：主集 husk（无需下载、有金标准）；泛化集 C4_kranz 水稻/玉米（跨项目数据，需提案获批）——是否同意
3. **技术栈确认**：v1 以 Python/scanpy 为执行栈；R/Seurat 是否需并行支持
4. **学生验证安排**：量化 CHECKLIST 模板可先出；独立复核由谁承担（另一名学生/我）
5. **小模型选型**：deepseek-v4-flash（现有 API）vs 本地部署更小模型（Qwen2.5-7B 级）
6. **原子任务清单确认**：T0-T17 是否覆盖所需，有无增删
7. **HITL 程度**：v2 起在哪些节点要求人工确认（建议：聚类后、注释初稿后）

---

## 九、交付物总清单（按阶段）

| 阶段 | 交付物 | 状态 |
|---|---|---|
| 0 | script-inventory.md（脚本→任务映射） | 待脚本集 |
| 1 | data/ 测试数据集（主集+泛化集+异常集） | 待确认数据 |
| 2 | 脚本模板 + 拆分定稿（含量化验证指标） | 待阶段 1 |
| 3 | 原子脚本 25-35 + skill 16-18 + golden tests + 学生验证/独立复核记录 | 核心阶段 |
| 4 | 中性流程 skill + 端到端报告（泛化集验证） | 待阶段 3 |
| 5 | 专家 skill + 小模型 benchmark 报告 | 待阶段 4 |
| 6 | 融合链路 + 知识蒸馏续作 | 长期 |

---

## 十、时间计划（并入自 TIME_PLAN.md v1.0，2026-08-20）

> 原独立文件 TIME_PLAN.md 已并入本方案，不再单独成文（文件连续性：时间计划是主方案的一节）。以下内容为原 TIME_PLAN.md v1.0 全量搬入，基准起点 2026-08-20，依据 PLAN.md v0.2 分阶段方案。
> ⚠️ v0.3 变更注记（排期影响待方案批注定稿后更新）：阶段 0 新增动作 B（行动空间文献提取，B1）约 +1-2 天；新增阶段 5b（消融评估，B5）约 +3-4 天，可与阶段 5 并行。

### 1. 总览

| 指标 | 方案 A（学生全职验证） | 方案 B（学生兼职，每天 2-3h） |
|---|---|---|
| 总周期 | **11 周**（8/20 – 11/4） | **14-15 周**（8/20 – 11 月底） |
| 关键路径 | 阶段 0 → 2 → 3 → 4 → 5（串行） | 同左，阶段 3 拉长 3-4 周 |
| 学生负载 | 阶段 3 全职 4 周 | 阶段 3 兼职 7-8 周 |
| 里程碑数 | M0-M6 | 同左 |

阶段 1（测试数据）与阶段 2（脚本研读）可并行；阶段 6（与领域专家融合+知识蒸馏）自 W9 起并行推进，不占关键路径。

### 2. 甘特图（方案 A，周粒度）

```
周次       W1   W2   W3   W4   W5   W6   W7   W8   W9   W10  W11
日期      8/20 8/27 9/3  9/10 9/17 9/24 10/1 10/8 10/15 10/22 10/29
─────────────────────────────────────────────────────────────────
阶段0 盘点 ████
阶段1 数据     ████
阶段2 研读         ████ ██
阶段3 开发+验证        ████ ████ ████ ████          ← 核心 4 周
  ├族A 数据层            ██
  ├族B 预处理              ███
  ├族C 整合/注释              ████
  └族D 下游/报告                  ███
阶段4 中性流程                              ████
阶段5 专家+benchmark                            ████ ████
阶段6 融合（并行）                                    ██████→ 长期
─────────────────────────────────────────────────────────────────
里程碑    M0   M1   M2      M3         M4    M5
Gate       ✔    ✔    ✔       ✔          ✔     ✔
```

图例：██ = 该周主要投入；族 A-D = 阶段 3 内四个并行任务族。

### 3. 分阶段排期明细

#### 阶段 0：资产收集与盘点（W1，8/20-8/26）
| 任务 | 天数 | 依赖 |
|---|---|---|
| 本地 70+ 脚本 + 3 套教程库先行盘点（不等脚本集） | 2 | 无 |
| 用户脚本集到位 → 清单化+任务映射（T0-T17 矩阵） | 3 | **用户交付脚本集** |
| 产出 script-inventory.md | 1 | 上 |
**Gate M0（W1 末）**：脚本→任务映射表定稿，缺口清单明确 → 用户确认后进阶段 2

#### 阶段 1：测试数据准备（W2，8/27-9/2，与阶段 2 并行）
| 任务 | 天数 | 依赖 |
|---|---|---|
| husk 主集：全量 8,980 + 2,000 子集（seed 固定） | 1 | 无 |
| 异常数据 5 类构造（缺基因/空矩阵/高叶绿体/低细胞数/非植物 ID） | 2 | 主集 |
| 泛化集：C4_kranz 水稻/玉米（跨项目提案获批后） | 2 | **用户批准** |
| data/README.md（来源/金标准/切割/异常说明） | 1 | 上 |
**Gate M1（W2 末）**：三套数据就绪，金标准列可追溯；泛化集未批则 W5 后补，不阻塞主线

#### 阶段 2：脚本研读与拆分（W3-W4 初，9/3-9/11）
| 任务 | 天数 | 依赖 |
|---|---|---|
| 70+ 脚本按任务族分组研读（方法/参数/已知坑） | 5 | M0 |
| 原子任务边界定稿（T0-T17，输入/输出/决策点/量化验证指标） | 2 | 上 |
| 统一脚本模板 TEMPLATE.py + 快速测试集跑通 | 2 | M1 |
**Gate M2（W4 初）**：拆分定稿+模板通过 → 冻结任务清单，用户确认后进阶段 3

#### 阶段 3：原子脚本开发 + 学生验证（核心，W4-W8，9/12-10/14）
四个任务族并行（每族独立流水：开发→基准输出→学生验证→独立复核→注册 skill）：

| 族 | 任务范围 | 脚本数 | 工期 |
|---|---|---|---|
| A 数据层 | T0-T2（导入/审计/ID 统一） | 5-6 | 1 周 |
| B 预处理 | T3-T7（QC/应激/周期/标准化/降维） | 6-7 | 1.5 周 |
| C 整合/聚类/注释 | T8-T12（批次/聚类/marker/注释/跨物种） | 7-8 | 2 周（最复杂） |
| D 下游/报告 | T13-T17（DEG/丰度/GO/轨迹/报告） | 6-7 | 1.5 周 |

- 每个脚本节奏：开发 0.5-1 天（Hermes/Claude Code）→ 基准输出 0.5 天 → **学生验证 0.5-1 天**（量化指标 ARI/F1/kBET 比对，CHECKLIST 模板）→ 独立复核 0.5 天（每族一轮，非本族开发者）
- 学生全职负载：约 18-22 个工作日（4 周）；兼职则拉长为 7-8 周（方案 B）
**Gate M3（W8 末）**：25-35 脚本 100% 注册 + 验证记录 100% + 异常数据失败路径通过

#### 阶段 4：中性流程组装 + 端到端（W9-W10 初，10/15-10/22）
| 任务 | 天数 | 依赖 |
|---|---|---|
| pipeline-scrna-standard 组装（固定顺序+决策节点） | 2 | M3 |
| 主集端到端（含 provenance/decision 日志验证） | 2 | 上 |
| 泛化集端到端（防过拟合验证） | 2 | 泛化集就绪 |
**Gate M4（W10 初）**：无人工干预完成率 ≥95%，泛化集跑通

#### 阶段 5：专家汇总 + 小模型 benchmark（W10-W11，10/23-11/4）
| 任务 | 天数 | 依赖 |
|---|---|---|
| 专家 skill 构建（任务解析→路由→降级链） | 3 | M4 |
| 指令集构建（50-100 条植物 scRNA 指令） | 2 | 上 |
| 小模型 benchmark 执行+与大模型对比 | 4 | 上 |
| 降级链验证+测试报告 | 2 | 上 |
**Gate M5（W11 末）**：完成率 ≥80% 或明确"未达标维持混合模式"；失败案例 100% 被拦截

#### 阶段 6：与植物领域专家融合（W9 起并行，长期）
- 编排层接线（plant-scrna-spatial-expert 路由指向本专家）：2-3 天
- 52 篇骨架文献知识蒸馏（原步骤 3b）：长期并行，另行排期
**Gate M6**：融合链路跑通（同任务融合前后结果一致+知识溯源）

### 4. 关键路径与瓶颈分析

1. **关键路径**：阶段 0 → 2 → 3 → 4 → 5。任一阶段延迟直接推后总交付。
2. **最大瓶颈：学生验证**（阶段 3）。开发可并行（族 A-D + Claude Code 委托），验证是人的环节不可压缩。缓解：
   - Hermes 先预跑一遍基准，学生专注核对逻辑与量化指标，不从零复现（省 30-50% 学生时间）
   - 验证清单模板化（CHECKLIST.md），逐项勾选，杜绝"看图差不多"
   - 族间流水：B 族开发时 A 族验证并行，不排队
3. **阻塞风险：用户脚本集**（阶段 0 输入）。缓解：本地 70+ 脚本先行盘点，脚本集到位后增量合并，最多损失 2 天。
4. **非阻塞项**：泛化集审批（可 W5 后补）、小模型达标率（内置混合模式兜底）。

### 5. 里程碑 Gate 汇总（用户决策点）

| 里程碑 | 时间 | 决策内容 |
|---|---|---|
| M0 | W1 末（8/26） | 脚本映射表确认，进阶段 2 |
| M1 | W2 末（9/2） | 测试数据方案确认（含泛化集审批） |
| M2 | W4 初（9/11） | 任务拆分冻结，进核心开发 |
| M3 | W8 末（10/14） | 原子脚本+验证全部通过，进组装 |
| M4 | W10 初（10/22） | 端到端达标，进专家汇总 |
| M5 | W11 末（11/4） | 专家交付 + 小模型结论 |
| M6 | 长期 | 融合链路 + 知识蒸馏 |

### 6. 可压缩选项（如需要提速）

| 压缩手段 | 节省 | 代价 |
|---|---|---|
| 阶段 3 族 A/B 与 C/D 更深度并行（学生 2 人） | 1-1.5 周 | 需第 2 名学生 |
| 异常数据构造与阶段 2 合并 | 0.5 周 | 阶段 2 密度增加 |
| 泛化集改用本地已有 C4_kranz 整合 h5ad（免下载） | 1-2 天 | 无（已含在方案） |
| 小模型 benchmark 减至 50 条指令 | 2 天 | 统计效力略降 |
| **不压缩项**：学生验证（压缩=质量风险）、端到端测试（压缩=交付可信度） |

---

## 附：v0.1 → v0.2 → v0.3 → v0.4 变更记录
- v0.2 采纳审核意见：确定性/数据依赖/判断三分层；植物特异任务补齐（叶绿体线粒体 QC、原生质体应激、细胞周期、双细胞、空滴、差异丰度、批次评估、分辨率稳定性、注释后审查）；验证指标量化（ARI/F1/kBET/iLISI）；数据泛化+异常数据；provenance 审计；小模型降级链+上线门槛；回归机制；HITL 渐进路线；跨物种降级为辅助 skill；输入格式归一化（T0）
- v0.3 融入 Biomni 思想（Science 2026, Stanford SNAP）：B1 行动空间文献提取（阶段 0 动作 B，E1 构建法）；B2 tools-manifest 注册表（动态工具检索，A1 ①，架构 L1.5 + 原子 skill 元数据 + 编排器检索入口）；B3 代码即行动强化（并行节点/条件分支）；B4 观测→修订闭环（机制表 + 编排器接口 + 熔断）；B5 消融评估（阶段 5b）；差异化声明（领域纵深 vs Biomni 通用广度）；资源策略声明（分析走代码、查询走知识库）
- v0.4 融入 LLM-as-a-Verifier 验证优先思维（Stanford Scaling Intelligence Lab, arXiv:2607.05391）：V1 生成-选择分离（T11 多候选注释 2-3 方案）；V2 标准分解评分（注释可信度五标准：marker 证据/聚类一致/文献一致/跨样本物种保守/生物学合理，1-5 分+证据，任一≤2 必改、均分<3.5 转人工）；V3 成本工程（评分复用 T10/T12 中间产物）；边界声明（验证不替代生成质量建设、终审仍是人工/大模型审查节点）；机制表新增"注释结果可信度"行
